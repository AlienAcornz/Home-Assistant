import FilterDropdown from "./FilterDropdown";

function NavBar() {
  return (
    <div>
        <button>Home</button>
        <button>Chat</button>
        <FilterDropdown/>
    </div>
  );
}

export default NavBar;
